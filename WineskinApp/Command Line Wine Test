#!/bin/bash
echo "";echo ""
cd "$(dirname "$0")"
export PATH="$PWD:$PATH"
cd "../../../Contents/Frameworks/wstools.bundle/bin"
export PATH="$PWD:$PATH"
cd "../../wswine.bundle/bin"
export PATH="$PWD:$PATH"
cd "../lib"
export WINESKIN_LIB_PATH_FOR_FALLBACK="$PWD:/opt/X11/lib:/opt/local/lib"
cd "../../"
export WINESKIN_LIB_PATH_FOR_FALLBACK="$WINESKIN_LIB_PATH_FOR_FALLBACK:$PWD:$HOME/Wineskin/lib:/usr/lib:/usr/libexec:/usr/lib/system:/usr/X11/lib:/usr/X11R6/lib"
# Export the stock fallback
export WINEDEBUG="err+all,warn-all,fixme+all,trace-all,fixme-esync"
export PS1="\nWineskin Command Line Wine Test\n\h:\W \u$ "
cd "$HOME"
echo "This is for testing normal Wine use only."
echo ""
echo "Default Wineprefix is ~/.wine and NOT the wrapper!"
echo ""
echo "If you are using a stock Wine engine then this is"
echo "    just like using normal command line Wine"
echo ""
/bin/bash
